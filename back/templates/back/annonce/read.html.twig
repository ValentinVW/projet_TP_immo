{% extends 'base.html.twig' %}

{% block title %}Annonces{% endblock %}

{% block body %}
    <h1>Annonce</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ annonce.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ annonce.name }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ annonce.content }}</td>
            </tr>
            <tr>
                <th>adresse</th>
                <td>{{ annonce.adresse }}</td>
            </tr>
             <tr>
                <th>Image</th>
                <td>{{ annonce.image }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ annonce.prix }}</td>
            </tr>
             <tr>
                <th>Surface</th>
                <td>{{ annonce.m² }}</td>
            </tr>
            <tr>
                <th>piece</th>
                <td>{{ annonce.piece }}</td>
            </tr>
            <tr>
                <th>Contact number</th>
                <td>{{ annonce.contactnumber }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ annonce.createdAt ? annonce.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Updated_at</th>
                <td>{{ annonce.updatedAt ? annonce.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('back_annonce_list') }}">back to list</a>

    <a href="{{ path('back_annonce_update', {'id': annonce.id}) }}">edit</a>

    {{ include('back/annonce/_delete_form.html.twig') }}
{% endblock %}
